#include <bits/stdc++.h>
using namespace std;

int main(int argc,char** argv) {
    char number[][10] = {
        "one",
        "two",
        "three",
        "four",
        "five",
    };

    for(int i=0; i<5; i++) {
        cout << number[i] << endl;
    }

    return 0;
}